# Screenshot placeholders

The docs reference these screenshots:

- `setup-wizard.png` — `/setup` first-run form.
- `admin-dashboard.png` — `/dashboard` (admin role).
- `qr-poster.png` — `/admin/jobs/<id>/qr-poster` rendered output.
- `admin-review.png` — `/admin/incidents/<id>` triage panel.
- `meeting-flow.png` — `/admin/meetings/<id>` mid-flow (pack drafted, not yet approved).
- `checkin-form.png` — `/checkin?job=<id>` (anonymous worker view on a phone-sized viewport).

Capture each at the noted route on a real install (1280×800 desktop or 390×844 mobile). PNG, no UI chrome (browser frame cropped). Drop them in this folder with the exact filenames above and remove this README.
